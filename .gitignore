# python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/

# builds
/build/
/dist/
/rust/target/
/.benchmarks/

# verification venv (created by the acceptance run, never committed)
/.venv_verify/

# OS / editor
Thumbs.db
.DS_Store
*.swp

# benchmarks/results/ IS committed: it is the published evidence.
# benchmarks/results/*.csv are regenerated by reproduce.py — regenerate before
# committing if you intentionally re-ran the suite on different hardware.
